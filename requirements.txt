# ============================================================
# Enterprise Knowledge Graph & Hybrid RAG Pipeline
# Pinecone + LlamaIndex + OpenAI + Cohere + Ragas
# Python 3.11+  |  pip install -r requirements.txt
# ============================================================
# ============================================================
# Enterprise Knowledge Graph & Hybrid RAG Pipeline
# FREE / LOCAL-MODEL EDITION
#
# Embedding:  BAAI/bge-small-en-v1.5  (HuggingFace, CPU, free)
# Reranking:  FlashRank ms-marco-MiniLM-L-12-v2  (CPU, free)
# Generation: Ollama llama3.2  (local, free)
# Vector DB:  Pinecone free tier  (1 index, 2 GB — only paid component)
#
# Only API key needed: PINECONE_API_KEY
# ============================================================
# --- Orchestration & RAG framework ---
llama-index==0.11.23
llama-index-core==0.11.23
llama-index-embeddings-huggingface==0.3.1
llama-index-llms-ollama==0.3.4
llama-index-vector-stores-pinecone==0.2.1
llama-index-readers-llama-parse==0.3.0


# --- Local embedding model (downloaded once, ~130 MB) ---
sentence-transformers==3.2.1
torch==2.4.1          # CPU-only; replace with torch==2.4.1+cu121 for CUDA GPU

# --- Local reranker (downloaded once, ~120 MB) ---
flashrank==0.2.9

# --- Document parsing ---
llama-parse==0.5.12

# --- Vector database (cloud) ---
pinecone-client==5.0.1

# --- Sparse keyword retrieval ---
rank-bm25==0.2.2



# --- Evaluation (Ragas 0.2.x — LlamaIndex-native wrappers) ---
# IMPORTANT: Ragas 0.2.x uses LlamaIndexLLMWrapper / LlamaIndexEmbeddingsWrapper.
# Do NOT install langchain-openai — the LangchainLLMWrapper path is 0.1.x only.
ragas==0.2.3
langchain==1.3.10
langchain-community==0.3.31
datasets==3.0.1

# --- API server ---
fastapi==0.115.5
uvicorn[standard]==0.32.0

# --- Utilities ---
python-dotenv==1.0.1
pydantic==2.9.2
tqdm==4.66.5
nltk==3.9.1
pandas==2.2.3
numpy==1.26.4
httpx==0.27.2
tenacity==8.5.0